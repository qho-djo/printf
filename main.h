#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>
#include <stdlib.h>
#include <stddef.h>
#include <stdio.h>

int _putchar(char c);
int _printf(const char *format, ...);
int _puts(char *c);
int __printf(const char *format, ...);

#endif
